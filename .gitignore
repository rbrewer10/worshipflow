node_modules
dist
dist-installer
out
.DS_Store
*.log
# Local media / data (not source)
/media
/data
# Demo/sample media — kept local, not committed (large binaries)
src/renderer/public/assets/*.mp4
*.sqlite
*.sqlite3
# Google Drive junk (this repo must live off Drive, but just in case)
desktop.ini
# Claude Code brainstorming-companion session scratch (mockups, server state)
.superpowers/
